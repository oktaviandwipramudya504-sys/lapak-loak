import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

// Kerangka dasar. Dijalankan dengan: npm install && npm run dev
export default defineConfig({
  plugins: [react()],
});
